import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const OrdersHistoryListContainer = styled.div `${tw `mt-8 flex-1 overflow-y-auto`}`;

export default OrdersHistoryListContainer;
